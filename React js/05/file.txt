useEffect in React JS (Simple & Proper Explanation)
🔹 What is useEffect?

👉 useEffect is a React Hook that is used to do some work after the component is shown on the screen.

🧠 Easy words:
useEffect runs code after React shows the UI.

🔹 Why do we use useEffect?
We use useEffect when we want to:
Call an API
Load data
Do something automatically
Run code after render

These are called side effects.
🔹 Basic Syntax of useEffect
useEffect(() => {
  // code to run
}, []);

Explanation:

First part () => {} → what work to do
Second part [] → when to do that work

✅ Simple API GET Request using useEffect
This is the most common and important use case.

🟢 Example: API GET request
import { useEffect, useState } from "react";
function Users() {
  const [users, setUsers] = useState([]);

  useEffect(() => {
    fetch("https://jsonplaceholder.typicode.com/users")
      .then((res) => res.json())
      .then((data) => {
        setUsers(data);
      });
  }, []);

  return (
    <div>
      <h2>User List</h2>
      {users.map((user) => (
        <p key={user.id}>{user.name}</p>
      ))}
    </div>
  );
}

export default Users;

🧠 Line-by-line Explanation (VERY IMPORTANT)
1️⃣ useState
const [users, setUsers] = useState([]);


users → store API data
setUsers → update data
[] → empty array because data comes later

2️⃣ useEffect
useEffect(() => {

👉 React says:
“Run this code after the component is shown”

3️⃣ API Call
fetch("https://jsonplaceholder.typicode.com/users")
API is called

Data is requested from server

4️⃣ Convert response to JSON
.then((res) => res.json())


Convert API response to usable data
5️⃣ Store data in state
.then((data) => {
  setUsers(data);
});


Save API data in users

React re-renders UI
6️⃣ Dependency Array
}, []);

This is VERY IMPORTANT 👇

✅ Dependency Array Explained (Easy)

The dependency array decides WHEN useEffect runs.

🟢 Case 1: Empty Dependency Array []
useEffect(() => {
  // API call
}, []);

🧠 Meaning:

Runs ONLY ONCE

When component loads first time

📌 Perfect for API GET request

🟡 Case 2: With Dependency [value]
useEffect(() => {
  console.log("Count changed");
}, [count]);

🧠 Meaning:

Runs when count changes

Also runs first time

🔴 Case 3: No Dependency Array
useEffect(() => {
  console.log("Runs every time");
});

🧠 Meaning:

Runs on every render

❌ Not good for API calls

✅ Why API Call Should Use [] ?

❌ Without []:

API will call again and again

Infinite loop

✅ With []:

API calls only once

Better performance

✅ Simple Real-Life Example for Students

🧠 Think like this:

Component loads → useEffect runs

API is called → data comes

Data is saved → UI updates

✅ Very Short Summary (For Students)

useEffect runs code after render

Used for API calls

Dependency array controls when it runs

[] means run once

[value] means run when value changes






import { useState, useEffect } from "react";

function Counter() {
  const [count, setCount] = useState(0);

  useEffect(() => {
    console.log("Count updated:", count);
  }, [count]);

  return (
    <div>
      <h2>Count: {count}</h2>
      <button onClick={() => setCount(count + 1)}>
        Increase
      </button>
    </div>
  );
}

export default Counter;
