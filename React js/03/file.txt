🚀 1. Functional Components
Easy Explanation

Functional components are simple JavaScript functions that return UI.

Example
function Welcome() {
  return <h1>Hello Students!</h1>;
}

Key Points

They are just functions → take inputs, return JSX.
They are simple, fast, and used everywhere.
They replace class components in modern React.

🚀 2. Component Props
Easy Explanation

Props = data passed from parent component → child component.
Think of it like parameters of a function.

Example
function Welcome(props) {
  return <h1>Hello {props.name}</h1>;
}

<Welcome name="Asim" />


👉 Here "Asim" is passed as props.name.

Key Points

Props are read-only (cannot be changed).

Used to customize components.

Child component receives props.

🚀 3. Props vs State
Feature	Props	State
Where data comes from	Parent	Inside the component
Can we change it?	❌ No	✔️ Yes
Purpose	Pass data	Store data that changes
Example	name="Ali"	setCount(count + 1)
Easy Summary

Props = external data (cannot change)

State = internal data (can change inside the component)

🚀 4. Reusable Components
Easy Explanation

Reusable components are components that we can use again and again with different data.

Example
function Button({ label }) {
  return <button>{label}</button>;
}

<Button label="Login" />
<Button label="Sign Up" />
<Button label="Submit" />


➡️ Same button, different text → reusable.

Key Points

Saves time

Less code → cleaner project

Easy to manage and maintain

🚀 5. Component Communication
⭐ A. Parent → Child (using props)

Parent sends data to child using props.

Example
function Parent() {
  return <Child message="Hello from parent!" />;
}

function Child({ message }) {
  return <p>{message}</p>;
}

⭐ B. Child → Parent (using callback function)

Child cannot directly change parent,
so parent sends a function as a prop → child calls it.

Example
function Parent() {
  function handleChildClick() {
    alert("Child clicked button!");
  }

  return <Child onClickChild={handleChildClick} />;
}

function Child({ onClickChild }) {
  return <button onClick={onClickChild}>Click Me</button>;
}

Explanation

Parent gives function to child.

Child calls that function.

Parent receives child's action.

🎉 Final Summary (Very Simple)
Topic	Easy 1-Line Definition
Functional Components	Components that are simple JavaScript functions.
Props	Data sent from parent → child.
State	Data inside component that can change.
Reusable Components	Components used multiple times with different data.
Communication	Parent → props, Child → callbacks.