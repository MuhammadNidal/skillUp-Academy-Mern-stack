1️⃣ Function Declaration
2️⃣ Function Expression
3️⃣ Arrow Function
4️⃣ Callback Function
5️⃣ IIFE
6️⃣ Async Function
7️⃣ Constructor Function
8️⃣ Higher-Order Function

At the end, I’ll show which ones are most used in modern JavaScript and why.

🟩 1. Function Declaration
🔹 What it is:

A normal named function, declared using the function keyword.

🔹 Syntax:
function functionName(parameters) {
  // code block
  return value;
}

🔹 Use Case:

✅ Use when you need a reusable, named function
✅ Perfect for utility functions, business logic, or repeated operations

🔹 Example:
// Utility function to calculate percentage
function calculatePercentage(total, value) {
  return (value / total) * 100;
}

// Reuse it anywhere
console.log(calculatePercentage(200, 50)); // Output: 25
console.log(calculatePercentage(500, 75)); // Output: 15

🔹 Why it’s important:

Hoisted — you can call it before it’s defined

Easy to read and maintain

Works well in all JS environments

🟩 2. Function Expression
🔹 What it is:

A function stored inside a variable.
Not hoisted (must be defined before use).

🔹 Syntax:
const greet = function(name) {
  return `Hello, ${name}`;
};

🔹 Use Case:

✅ Use when you need to pass functions around as data
✅ Great for callbacks, or when defining functions inside another function

🔹 Example:
const getGreeting = function(name) {
  return `Hello, ${name}!`;
};

console.log(getGreeting("Ali"));

🔹 Why it’s useful:

Helps limit scope (keeps function local to a block)

Useful for modular design or event handling

🟩 3. Arrow Function (🔥 MOST USED in Modern JS)
🔹 What it is:

A shorter and cleaner function syntax introduced in ES6 (2015).

🔹 Syntax:
const functionName = (params) => expression;


If multiple lines:

const functionName = (params) => {
  // statements
  return value;
};

🔹 Use Case:

✅ Perfect for:

Callbacks (map, filter, forEach, etc.)

Short utility functions

React components

Anywhere you want clean, short syntax

🔹 Example 1 — Simple One-Liner:
const add = (a, b) => a + b;
console.log(add(5, 3)); // Output: 8

🔹 Example 2 — With Array Methods:
const numbers = [1, 2, 3, 4, 5];

// Double every number
const doubled = numbers.map(num => num * 2);
console.log(doubled); // [2, 4, 6, 8, 10]

🔹 Example 3 — Arrow Function + this Behavior:

Arrow functions don’t create their own this — they use the this from the outer scope.

const person = {
  name: "Nidal",
  greet: function() {
    setTimeout(() => {
      console.log(`Hello, my name is ${this.name}`);
    }, 1000);
  }
};

person.greet();
// Output after 1s: Hello, my name is Nidal


If you used a normal function in setTimeout, this.name would be undefined or refer to the global scope.

🟩 4. Callback Function
🔹 What it is:

A function passed as an argument to another function.

🔹 Use Case:

✅ Used heavily in:

Event handling

Asynchronous tasks (API calls, timeouts)

Array methods

🔹 Example — Simple Callback:
function greetUser(callback) {
  console.log("Hello!");
  callback();
}

function sayBye() {
  console.log("Goodbye!");
}

greetUser(sayBye);

// Output:
// Hello!
// Goodbye!

🔹 Example — Callback with Data:
function processUserData(name, callback) {
  console.log(`Processing data for ${name}...`);
  callback(name);
}

processUserData("Nidal", (user) => {
  console.log(`Welcome ${user}!`);
});

🟩 5. IIFE (Immediately Invoked Function Expression)
🔹 What it is:

A function that runs immediately after it’s created.

🔹 Syntax:
(function() {
  console.log("I run immediately!");
})();

🔹 Use Case:

✅ When you want to:

Create a private scope

Avoid global variables

Initialize something immediately

🔹 Example:
(function() {
  const message = "Private variable";
  console.log(message);
})();

console.log(typeof message); // undefined (not accessible)

🟩 6. Async Function (🔑 Modern Web Use)
🔹 What it is:

A function that allows asynchronous operations using await.

🔹 Use Case:

✅ Used for:

Fetching data from APIs

Waiting for promises

Writing clean async code

🔹 Example:
async function fetchUser() {
  console.log("Fetching user data...");

  const response = await fetch("https://jsonplaceholder.typicode.com/users/1");
  const user = await response.json();

  console.log("User fetched:", user.name);
}

fetchUser();


🔹 Benefits:

Makes async code look synchronous

Easier error handling with try...catch

🟩 7. Constructor Function
🔹 What it is:

Used to create multiple objects with the same structure before class syntax existed.

🔹 Syntax:
function Person(name, age) {
  this.name = name;
  this.age = age;
}

const user1 = new Person("Nidal", 24);
const user2 = new Person("Ali", 22);

console.log(user1.name); // Nidal

🔹 Use Case:

✅ When you want to create object blueprints
✅ Still seen in older JS codebases

🟩 8. Higher-Order Function (HOF)
🔹 What it is:

A function that takes another function as an argument or returns another function.

🔹 Use Case:

✅ Used in:

Array methods (map, filter, reduce)

React hooks

Functional programming patterns

🔹 Example:
function multiplier(factor) {
  return function(number) {
    return number * factor;
  };
}

const double = multiplier(2);
const triple = multiplier(3);

console.log(double(4)); // 8
console.log(triple(4)); // 12

🟦 Most Commonly Used in Modern JavaScript
Function Type	Common Usage	Modern Use
Function Declaration	Utility logic	✅ Common
Function Expression	Anonymous / inline	⚙️ Moderate
Arrow Function	React, callbacks, array methods	🔥 Most used
Async Function	API calls, backend, Node.js	🔥 Most used
IIFE	Private scope / config	⚙️ Moderate
Callback / HOF	Event, iteration, async control	🔥 Common
Constructor	Older object creation	⚙️ Less now (Replaced by class)
🧩 Summary Example: Combining All Concepts
// 1. Function Declaration
function add(a, b) {
  return a + b;
}

// 2. Arrow Function
const square = n => n * n;

// 3. Callback Function
function processNumber(num, callback) {
  const result = callback(num);
  console.log("Processed:", result);
}

// 4. IIFE for initialization
(() => console.log("App starting..."))();

// 5. Async Function for data fetch
async function getUser() {
  const res = await fetch("https://jsonplaceholder.typicode.com/users/2");
  const data = await res.json();
  console.log("Fetched user:", data.name);
}

// Run everything
console.log(add(2, 3));
processNumber(5, square);
getUser();


✅ Key Takeaways:

Use Arrow Functions for short and callback logic.

Use Async Functions for API calls or async code.

Use Normal Functions for reusable named logic.

Use IIFEs to isolate scope.

Use HOFs for cleaner, reusable logic with functions as data.