🧮 1. Arithmetic Operators in JavaScript

These are used to perform math operations like addition, subtraction, multiplication, etc.

✨ Common Arithmetic Operators:

Operator	Name	Example	Output
+	Addition	5 + 2	7
-	Subtraction	10 - 3	7
*	Multiplication	4 * 3	12
/	Division	12 / 4	3
%	Modulus	10 % 3	1 (Remainder)
++	Increment	x++	Increases by 1
--	Decrement	x--	Decreases by 1

💡 Example:

let a = 10;
let b = 3;

console.log(a + b);  // 13
console.log(a - b);  // 7
console.log(a * b);  // 30
console.log(a / b);  // 3.333...
console.log(a % b);  // 1

🔍 2. Comparison Operators in JavaScript

These are used to compare two values. They return true or false.

✨ Common Comparison Operators:

Operator	Name	Example	Output
==	Equal to	5 == "5"	true
===	Strict Equal (value + type)	5 === "5"	false
!=	Not Equal	10 != 7	true
!==	Strict Not Equal	10 !== "10"	true
>	Greater than	8 > 5	true
<	Less than	3 < 7	true
>=	Greater than or equal	10 >= 10	true
<=	Less than or equal	4 <= 5	true

💡 Example:

let x = 5;
let y = "5";
let z = 8;

console.log(x == y);   // true (only values compared)
console.log(x === y);  // false (different types)
console.log(z > x);    // true
console.log(z <= x);   // false

📝 3. Assignment Operators in JavaScript

These are used to assign values to variables.

✨ Common Assignment Operators:

Operator Name	Example	Equivalent To
=	Assign	x = 10	x = 10
+=	Add and assign	x += 5	x = x + 5
-=	Subtract and assign	x -= 3	x = x - 3
*=	Multiply and assign	x *= 2	x = x * 2
/=	Divide and assign	x /= 4	x = x / 4
%=	Modulus and assign	x %= 3	x = x % 3

💡 Example:

let num = 10;

num += 5;   // num = 15
console.log(num);

num -= 3;   // num = 12
console.log(num);

num *= 2;   // num = 24
console.log(num);

num /= 4;   // num = 6
console.log(num);

num %= 5;   // num = 1
console.log(num);

⚡ 4. Logical Operators in JavaScript

Used to combine or invert conditions (returns true or false).

Operator	Name	Example	Output
&&	Logical AND	(5 > 3 && 10 > 7)	true
||	Logical OR	(5 > 3 || 10 < 7)	true
!	Logical NOT	!(5 > 3)	false

💡 Example:

let age = 20;
let hasID = true;

console.log(age > 18 && hasID); // true (both conditions true)
console.log(age > 18 || hasID); // true (at least one is true)
console.log(!(age > 18));       // false (negates the result)