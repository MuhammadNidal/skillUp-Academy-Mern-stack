🔹 1. Synchronous (Sync) JavaScript

Meaning:
JavaScript runs line by line, one after another.
It waits for each line to finish before moving to the next one.

If one task takes too long, it blocks the rest of the code.

Example:

console.log("Start");
                              
function syncFunc() {
  console.log("Inside sync function");
}

syncFunc();

console.log("End");


🟢 Output:

Start
Inside sync function
End


👉 The code runs step-by-step — nothing happens until the previous line is done.

🔹 2. Asynchronous (Async) JavaScript

Meaning:
Async code does not wait.
It runs in the background and lets other code continue running.

Used for:

API calls

Timers (like setTimeout)

Reading files

Example:

console.log("Start");

setTimeout(() => {
  console.log("Inside async function");
}, 2000);

console.log("End");


🟢 Output:

Start
End
Inside async function   ← (after 2 seconds)


👉 JavaScript didn’t wait for the 2 seconds — it kept going!

🔹 3. Promises in JavaScript

A Promise means:
👉 “I’ll give you the result later — success or error.”

States of a Promise:

⏳ Pending — still working

✅ Resolved — finished successfully

❌ Rejected — failed with an error

Example:

const promise = new Promise((resolve, reject) => {
  let success = true;

  if (success) {
    resolve("Task completed!");
  } else {
    reject("Something went wrong.");
  }
});

promise
  .then((result) => console.log(result))   // success
  .catch((error) => console.log(error));   // error

🧠 Example with Delay
function getData() {
  return new Promise((resolve, reject) => {
    setTimeout(() => {
      resolve("Data loaded!");
    }, 2000);
  });
}

getData()
  .then((data) => console.log(data))
  .catch((err) => console.log(err));


🟢 Output:

Data loaded! (after 2 seconds)

🔹 4. Async / Await (Simpler Way to Use Promises)

async and await make async code look like normal, step-by-step code.

Keyword	Meaning	Used Inside
async	Makes function return a Promise	Before function
await	Waits for Promise to finish	Inside async function
Example:
function fetchData() {
  return new Promise((resolve) => {
    setTimeout(() => {
      resolve("Data loaded");
    }, 2000);
  });
}

async function showData() {
  console.log("Fetching...");
  const result = await fetchData(); // waits here until done
  console.log(result);
  console.log("Done");
}

showData();


🟢 Output:

Fetching...
(wait 2 seconds)
Data loaded
Done


👉 Looks simple — but still asynchronous!

🔹 5. setTimeout(), setInterval(), and clearInterval()
⏰ setTimeout → Run Once After Delay
console.log("Start");

setTimeout(() => {
  console.log("This runs after 3 seconds");
}, 3000);

console.log("End");


🟢 Output:

Start
End
This runs after 3 seconds

🔁 setInterval → Repeats Again and Again
let count = 1;

const intervalId = setInterval(() => {
  console.log("Running... " + count);
  count++;
}, 1000);


🟢 Output:

Running... 1
Running... 2
Running... 3
...

🛑 clearInterval → Stop Repeating
let count = 1;

const intervalId = setInterval(() => {
  console.log("Running... " + count);
  count++;
}, 1000);

setTimeout(() => {
  clearInterval(intervalId);
  console.log("Stopped!");
}, 5000);


🟢 Output:

Running... 1
Running... 2
Running... 3
Running... 4
Running... 5
Stopped!

🔹 6. JSON.stringify()

Converts a JavaScript object → into a JSON string (text).

Why:
When sending data to a server (API), it must be in string format.

Example:

const user = {
  name: "Nidal",
  age: 22
};

const jsonString = JSON.stringify(user);

console.log(jsonString);


🟢 Output:

{"name":"Nidal","age":22}

🔹 7. fetch() — Making API Calls

fetch() is used to send HTTP requests (like GET, POST) to servers.
It returns a Promise.

🟢 GET Request Example
fetch('https://jsonplaceholder.typicode.com/posts/1')
  .then(response => response.json())
  .then(data => console.log("Data received:", data))
  .catch(error => console.error("Error:", error));


Output:

Data received: { userId: 1, id: 1, title: "...", body: "..." }

🟢 GET Request (with async/await)
async function getPost() {
  try {
    const response = await fetch('https://jsonplaceholder.typicode.com/posts/1');
    const data = await response.json();
    console.log("Post:", data);
  } catch (error) {
    console.error("Fetch error:", error);
  }
}

getPost();

🟢 POST Request Example (Send Data to Server)
fetch('https://jsonplaceholder.typicode.com/posts', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    title: 'My Post',
    body: 'This is the post content.',
    userId: 1
  })
})
  .then(response => response.json())
  .then(data => console.log("Post created:", data))
  .catch(error => console.error("Error:", error));

🎯 Summary for Students
Concept	Description	Example
Sync	Runs line by line	Normal functions
Async	Doesn’t block code	setTimeout, API calls
Promise	Handle async result (success/error)	.then().catch()
async/await	Cleaner way to use Promises	await fetch()
setTimeout	Run once after delay	setTimeout(fn, 1000)
setInterval	Run repeatedly	setInterval(fn, 1000)
clearInterval	Stop repeating	clearInterval(id)
JSON.stringify	Object → String	Send data to API
fetch()	Make API requests	GET / POST