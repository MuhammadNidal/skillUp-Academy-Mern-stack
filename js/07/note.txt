🌳 1. Introduction to the DOM
📘 What is the DOM?

DOM stands for Document Object Model.
When a browser loads your HTML file, it creates a model (structure) of your web page in memory.
This model looks like a tree structure, where every HTML tag is a node (object) — for example:

<html>
 └── <body>
      ├── <h1>
      └── <p>


So:

<html> is the root node

<body> is a child node of <html>

<h1> and <p> are children of <body>

💡 Think of it like a family tree — you can travel from parent to child and vice versa.

⚙️ Why is the DOM Important?

Because JavaScript uses the DOM to make web pages interactive.

For example, using JavaScript + DOM you can:

✅ Change text and images

✅ Change CSS styles

✅ Add or remove HTML elements

✅ Respond to user actions (like button clicks or input typing)

So whenever you interact with a website — click a button, open a dropdown, submit a form — you are actually interacting with the DOM via JavaScript.

🧭 2. How to Access DOM Elements

To manipulate elements, you must select them first.

JavaScript provides several DOM selector methods.

🎯 Main DOM Selectors
Method	Description
document.getElementById("id")	Selects a single element by its ID
document.getElementsByClassName("class")	Selects all elements with that class
document.getElementsByTagName("tag")	Selects all elements with that tag
document.querySelector("css selector")	Selects the first element that matches the CSS selector
document.querySelectorAll("css selector")	Selects all elements that match the CSS selector
🔍 Examples and Explanation
1️⃣ getElementById()

HTML

<p id="myPara">Hello World!</p>


JavaScript

const para = document.getElementById("myPara");
para.textContent = "Text changed using ID!";
para.style.color = "blue";


🧠 Explanation

getElementById() finds one element by its ID.

textContent changes the text inside it.

style.color changes the color.

💡 Each ID should be unique on a page.

2️⃣ getElementsByClassName()

HTML

<p class="myText">First</p>
<p class="myText">Second</p>


JavaScript

const elements = document.getElementsByClassName("myText");
elements[0].textContent = "Changed First";
elements[1].style.color = "green";


🧠 Explanation

Returns multiple elements (HTMLCollection).

You use indexing to access specific ones ([0], [1], etc.).

Can loop through them if needed.

3️⃣ getElementsByTagName()

HTML

<h2>This is heading</h2>
<h2>Another heading</h2>


JavaScript

const headings = document.getElementsByTagName("h2");
headings[0].textContent = "Heading Changed!";
headings[1].style.backgroundColor = "lightgray";


🧠 Explanation

Selects all tags with a specific name (<h2> in this case).

Returns a collection like an array.

4️⃣ querySelector()

HTML

<div class="box">This is a box</div>


JavaScript

const box = document.querySelector(".box");
box.textContent = "Selected with querySelector!";
box.style.border = "2px solid red";


🧠 Explanation

Uses CSS selector syntax (.class, #id, or tag).

Only selects the first matching element.

Example:

document.querySelector("#id")
document.querySelector(".class")
document.querySelector("p")

5️⃣ querySelectorAll()

HTML

<p class="note">Note 1</p>
<p class="note">Note 2</p>


JavaScript

const notes = document.querySelectorAll(".note");
notes[0].textContent = "Updated Note 1";
notes[1].style.color = "purple";


🧠 Explanation

Returns a NodeList (like an array).

You can access items by index or use .forEach() to loop:

notes.forEach(note => note.style.fontWeight = "bold");

🎨 Changing Text and Style
🔤 Changing Text:
element.textContent = "New Text Here";
element.innerHTML = "<b>Bold Text</b>"; // allows HTML tags


textContent → changes only text

innerHTML → allows HTML inside

🎨 Changing Style:
element.style.color = "red";
element.style.backgroundColor = "yellow";
element.style.fontSize = "20px";


You can modify any CSS property using element.style.

🧪 Live Mini Example

HTML

<h1 id="main">Hello</h1>
<p class="info">Old Text</p>
<button onclick="changeIt()">Change Content</button>


JavaScript

function changeIt() {
  document.getElementById("main").textContent = "Changed Heading!";
  document.querySelector(".info").style.color = "blue";
}


🧠 Explanation for Students:

When you click the button, it calls changeIt().

The function changes:

The heading text (textContent)

The paragraph color (style.color)

So the web page updates dynamically without refreshing — this is the power of DOM manipulation.

🧩 Summary for Teaching
Concept	Description	Example
DOM	Tree structure of the web page	HTML → Objects
Why	JS uses it to change content/style	element.textContent
getElementById	Select by ID	#id
getElementsByClassName	Select by class	.class
getElementsByTagName	Select by tag	h1, p
querySelector	First match	.box, #main
querySelectorAll	All matches	.note