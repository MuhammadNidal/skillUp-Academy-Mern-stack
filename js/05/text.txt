JavaScript Loops – Easy Explanation
🌟 What is a Loop?

A loop lets you repeat a block of code multiple times — automatically — instead of writing it again and again.

🥇 1. for Loop

✅ Best for: When you know how many times to run the code.

📘 Syntax:

for (initialization; condition; increment/decrement) {
  // code to run
}

📗 Example:

for (let i = 1; i <= 5; i++) {
  console.log("Step:", i);
}
📊 How it works:

Step	i value	Condition	Output
1	1	true	Step: 1
2	2	true	Step: 2
...	...	...	...
5	5	true	Step: 5
6	6	false	Stops loop

💡 Tips:

You can count backward → for (let i = 5; i >= 1; i--)

You can skip numbers → for (let i = 0; i <= 10; i += 2)

🥈 2. while Loop

✅ Best for: When you don’t know how many times it should run.

📘 Syntax:

while (condition) {
  // code to run
}


📗 Example:

let i = 1;
while (i <= 5) {
  console.log("While Step:", i);
  i++;
}


⚠️ Be careful!
If you forget to change i, it becomes an infinite loop 😵:

while (true) {
  // never stops unless break
}

🥉 3. do...while Loop

✅ Best for: When the code must run at least once before checking the condition.

📘 Syntax:

do {
  // code to run
} while (condition);


📗 Example:

let i = 6;
do {
  console.log("Do While Step:", i);
  i++;
} while (i <= 5);


🧾 Output:

Do While Step: 6


💡 Why?
Because it runs first, then checks the condition later.

🧱 4. for...in Loop

✅ Best for: Looping over object properties (keys).

📘 Syntax:

for (let key in object) {
  console.log(key, object[key]);
}


📗 Example:

const user = { name: "Nidal", age: 22, country: "Pakistan" };

for (let key in user) {
  console.log(`${key}: ${user[key]}`);
}


🧾 Output:

name: Nidal
age: 22
country: Pakistan


💡 Note:
It loops over keys — not values.
Can be used on arrays, but not ideal.

🧩 5. for...of Loop

✅ Best for: Looping over values in arrays, strings, maps, sets, etc.

📘 Syntax:

for (let item of iterable) {
  console.log(item);
}


📗 Example 1 (Array):

const colors = ["red", "green", "blue"];
for (let color of colors) {
  console.log(color);
}


🧾 Output:

red
green
blue


📗 Example 2 (String):

for (let char of "Nidal") {
  console.log(char);
}


🧾 Output:

N
i
d
a
l


💡 Note:

Works with iterables (arrays, strings)

Doesn’t work directly on objects — use:

for (let [key, value] of Object.entries(obj)) { ... }

🧾 Final Comparison Table
Loop Type	Use Case	Works With	Runs At Least Once?
for	Known count	Any logic	❌
while	Unknown count (check before run)	Any logic	❌
do...while	Run at least once	Any logic	✅
for...in	Loop over keys	Objects	❌
for...of	Loop over values	Arrays, Strings, etc.	❌
🎓 How to Teach This to Students

✅ Step-by-step plan:

Start with a real example: “I want to print my name 5 times — should I write it 5 times or use a loop?”

Explain for loop first (most common).

Show while next (condition-based).

Explain do...while (guaranteed one-time run).

Introduce for...in and for...of for objects and arrays.

End with a small practice challenge:

// Print numbers 1 to 10 but skip 5
for (let i = 1; i <= 10; i++) {
  if (i === 5) continue;
  console.log(i);
}
